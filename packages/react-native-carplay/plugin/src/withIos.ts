import { ConfigPlugin } from 'expo/config-plugins';

export const withIosCarPlay: ConfigPlugin = (config) => {
  throw new Error(
    'react-native-carplay iOS configuration is not implemented yet. Please configure iOS manually or wait for future updates.'
  );
};